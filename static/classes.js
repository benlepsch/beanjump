class Carrot {
    constructor(velocity) {
        this.velocity = velocity; // negative velocity = going left
        if (this.velocity < 0) {
            // come from right side
        } else {
            // come from left side
        }
    }
}